# Fixtures

- `sample/` — synthetic, committed, used by tests. Shapes match Bee docs as of 2026-09-13, not a live account.
- `real/` — gitignored. Output of `scripts/capture-fixtures.sh`. Phase 1+ replay source.

See [docs/FIXTURES.md](../docs/FIXTURES.md).
